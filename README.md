# Student Mental Health and Academic Performance Analysis

A reproducible multiclass machine-learning project that examines how student demographics, screen time, sleep, physical activity, stress, and exam anxiety relate to reported academic-performance changes during online learning.

The model predicts three outcomes without collapsing their meaning:

- `Improved`
- `Same`
- `Declined`

## Why this version is reliable

- Splits the data before fitting any preprocessing step.
- Uses a single scikit-learn `Pipeline` with `ColumnTransformer`.
- Standardizes numeric variables using training data only.
- One-hot encodes categorical variables and safely handles unseen categories.
- Uses a stratified 80/20 train-test split.
- Compares logistic regression against a most-frequent baseline.
- Reports accuracy, balanced accuracy, macro F1, weighted precision, and weighted recall.
- Runs five-fold cross-validation over the complete preprocessing/model pipeline.
- Calculates permutation importance only on held-out test data.

## Dataset

Expected filename:

```text
Student Mental Health Analysis During Online Learning.csv
```

The CSV and original ZIP are ignored by Git. Before publishing, document the dataset's original source, author, license, and download URL here. Do not commit data unless its license explicitly allows redistribution.

Expected columns:

- Name
- Gender
- Age
- Education Level
- Screen Time (hrs/day)
- Sleep Duration (hrs)
- Physical Activity (hrs/week)
- Stress Level
- Anxious Before Exams
- Academic Performance Change

`Name` is treated only as an identifier and is excluded from modeling.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run the analysis

```bash
python run_analysis.py
```

Custom paths are supported:

```bash
python run_analysis.py --data /path/to/data.csv --output-dir artifacts
```

Generated artifacts:

- `artifacts/metrics.json` - dataset profile, split details, test metrics, baseline, cross-validation, feature importance, and limitations
- `artifacts/data_quality.json` - missing-value counts, duplicate count, dtypes, numeric ranges, and categorical cardinality
- `artifacts/eda_summary.json` - target balance, numeric means by outcome, and numeric correlations
- `artifacts/target_distribution.png` - class-balance overview for academic-performance change
- `artifacts/numeric_features_by_target.png` - numeric feature distributions grouped by target outcome
- `artifacts/confusion_matrix.png` - held-out test confusion matrix
- `artifacts/feature_importance.png` - permutation importance on held-out test data
- `artifacts/model.joblib` - fitted preprocessing and classification pipeline

## Current results

Using a fixed, stratified 80/20 split (`random_state=42`):

| Metric | Logistic regression | Majority baseline |
|---|---:|---:|
| Accuracy | 0.2800 | 0.4000 |
| Balanced accuracy | 0.2972 | 0.3333 |
| Macro F1 | 0.2731 | 0.1905 |
| Weighted precision | 0.2843 | 0.1600 |
| Weighted recall | 0.2800 | 0.4000 |

Five-fold training-set cross-validation produced a mean macro F1 of **0.3078 ± 0.0221**.

The model predicts minority classes more evenly than the majority baseline, which raises macro F1, but its accuracy and balanced accuracy remain low. Permutation importance is also close to zero for every feature. The responsible conclusion is that this dataset does **not** provide strong out-of-sample predictive signal for academic-performance change. That negative result is more useful than presenting the earlier binary 60.5% accuracy without a baseline or leakage controls.

## Run tests and coverage

```bash
pytest --cov=mental_health_pipeline --cov-report=term-missing --cov-fail-under=80
```

The test suite covers schema validation, column normalization, data-quality and EDA profiling, preservation of all three target classes, unseen categories, end-to-end training, and artifact generation.

## Interpretation boundaries

This is an observational learning project, not a diagnostic or causal model. Associations in the dataset do not establish that screen time, sleep, activity, stress, or anxiety causes academic performance to change. Self-reported responses can contain recall and response bias, and external validation is required before using the model in another institution or population.

## Repository structure

```text
.
├── mental_health_pipeline.py
├── run_analysis.py
├── mentl_health.ipynb
├── requirements.txt
├── tests/
│   └── test_pipeline.py
├── .github/workflows/tests.yml
└── artifacts/                 # generated by run_analysis.py
```

The original notebook is retained for exploratory context. `run_analysis.py` is the reproducible, leakage-safe execution path.
